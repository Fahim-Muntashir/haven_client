import React from 'react';

const page = ({ params }: { params: {} }) => {

    return (
        <section>
            <div>
                <div>

                </div>
                <div>

                </div>
            </div>

        </section>
    );
};

export default page;